add a bootstrap period where any node can register for free and recieves a virtual balanced staked of 10000 tokens (he cannot transfer during bootstrap period), up to a maximum of 500 active nodes. Any number of nodes can register during bootstrap, but if the become inactive they are going to be slashed and loose their tokens! So 500 is the maximum of active nodes during boostrap, enforced on consensus (ordered by total staked balance, initial virtual + rewards). The rewards are automatically staked for the node (the rewards can be unstaked and transfer/sold to users). After bootstrap period the virtual balance becomes materialized (if there were are active and not slashed they get the 100000 otkens plus the mining rewards they accumulated) ended the nodes are only selected if they are registered and have more than 0.1% of the total emitted tokens. During bootstrap period the per block rewards are prorated to be 24% per year, and after boostrap period they are 12% per year, but during bootstrap period they can transfer and spend the blocks rewards but not they cannot transfer or spend the virtual staking balance 10000 tokens they got on registration.

can we remove the Monotonic Loss Decrease restriction (add a flag to activate that optionally) and focus on Merkle Proofs and Quantized Gradient verification as the main consensus security features???

